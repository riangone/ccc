@* ファイル概要: ダッシュボード画面。config/dashboard.yml で定義した統計カードを表示します。
   このビューは画面表示と操作導線を担当します。
   統計項目の追加・変更は config/dashboard.yml を編集してください。 *@

@model List<DynamicCrudSample.Controllers.DashboardStatViewModel>
@{
    ViewData["Title"] = "Dashboard";
}

<div class="space-y-6">
    <h1 class="text-2xl font-bold">Dashboard</h1>

    @if (Model != null && Model.Count > 0)
    {
        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
            @foreach (var stat in Model)
            {
                <div class="stat bg-base-100 shadow rounded-box">
                    @if (!string.IsNullOrEmpty(stat.Icon))
                    {
                        <div class="stat-figure text-4xl">@stat.Icon</div>
                    }
                    <div class="stat-title text-xs">@stat.Label</div>
                    <div class="stat-value text-2xl">@stat.Value</div>
                    @if (!string.IsNullOrEmpty(stat.Color))
                    {
                        <div class="stat-desc">
                            <span class="badge @stat.Color badge-sm">@stat.Label</span>
                        </div>
                    }
                </div>
            }
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <span>統計情報が設定されていません。<code>config/dashboard.yml</code> に統計定義を追加してください。</span>
        </div>
    }
</div>
