@* ファイル概要: ホーム画面の説明表示です。
   このビューは画面表示と操作導線を担当します。
   データバインディング名や hx-* 属性を変更する場合は、コントローラ側との整合を必ず確認してください。 *@

@{
    ViewData["Title"] = "Home";
}

<div class="hero bg-base-100 rounded-box shadow">
    <div class="hero-content text-center py-16">
        <div class="max-w-xl">
            <h1 class="text-4xl font-bold">Dynamic CRUD + Chinook</h1>
            <p class="py-4">Authentication, localization, audit logging, and metadata-driven CRUD over Chinook DB.</p>
            <a class="btn btn-primary" asp-controller="DynamicEntity" asp-action="Index" asp-route-entity="customer">Open Customer</a>
        </div>
    </div>
</div>
